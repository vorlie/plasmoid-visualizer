# Fonts

Place bundled application fonts in this directory. If
`AdwaitaMono-Regular.ttf` is present, the visualizer uses it for its overlay.
Otherwise it falls back to Consolas on Windows and common system monospace
fonts on Linux.
